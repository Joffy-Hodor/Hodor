@model HodorTutor.Models.ViewModels.Announces.AnnounceView
@{
    ViewBag.Title = "Create";
}

<h1 class="header" style="margin-top:0px; padding-left:0px"><i class="fa fa-pencil fa-fw"></i>Post</h1>
<div> 
    @using(Html.BeginForm()){
    <table class="table">
        @Html.ValidationSummary(true)
		<tr>
            <td style="width:20%">
			@Html.LabelFor(e => e.Title)
            </td>
            <td>
			@Html.TextBoxFor(e => e.Title, new { @style = "width:70%;", maxlength = 50 })<br />
			@Html.ValidationMessageFor(e => e.Title)     
            </td>
        </tr>
        <tr>
            
            <td>
			@Html.LabelFor(e => e.Detail)
            </td>
            <td>
                <div class="btn-group col-xs-8" style="padding:0px" >
                    <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-align-left"></span></button>
                    <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-align-center"></span></button>
                    <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-align-right"></span></button>
                    <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-align-justify"></span></button>
                </div>
                <div class="col-sm-13">
                    @Html.TextAreaFor(e => e.Detail, new { @style = "width:70%; height:150px", @type = "text", })<br />
			        @Html.ValidationMessageFor(e => e.Detail)  
                </div>			   
		    </td> 
		</tr>
        <tr><td><input type="submit" value="ตั้งประกาศ" class="btn btn-default" /></td><td></td></tr>
    </table>
	}
</div>

<script>
    $(document).ready(function () {
        $('#p1').css('display', 'inline');
        console.log('mySetMenuPic');
    });
</script>
